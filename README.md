# thermostat-qt-esp32
Desktop thermostat controller built with Qt/QML and C++, communicating with an ESP32 over BLE. Includes SQLite persistence, unit-tested core logic via dependency injection, and a NimBLE-based firmware.

qt, qml, cpp, esp32, ble, sqlite, iot
